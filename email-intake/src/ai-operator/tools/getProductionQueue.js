"use strict";

/** Phase 1 stub — production board / Dataverse in a future phase (READ_ONLY). */
async function handler(/* params */ _params = {}) {
  return {
    status: "NOT_IMPLEMENTED_PHASE1",
    message: "Production queue tool not wired in Phase 1.",
  };
}

module.exports = { handler };
