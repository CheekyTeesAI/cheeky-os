"use strict";

/** Phase 1 stub — Square/query integration later (READ_ONLY). */
async function handler(/* params */ _params = {}) {
  return {
    status: "NOT_IMPLEMENTED_PHASE1",
    message: "Late invoice listing will use Square + internal rules in a future phase.",
  };
}

module.exports = { handler };
