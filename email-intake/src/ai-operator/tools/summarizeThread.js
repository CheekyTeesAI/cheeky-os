"use strict";

/** Phase 1 stub — threading + summarization is a later intent. */
async function handler(/* params */ _params = {}) {
  return {
    status: "NOT_IMPLEMENTED_PHASE1",
    message: "summarizeThread not registered in Phase 1 operator router.",
  };
}

module.exports = { handler };
