"use strict";

/**
 * Intent parsing (stub v1).
 * Future: natural language → normalized intent + params for ChatGPT ↔ Operator Bridge.
 */

function parseIntentPlaceholder(_naturalLanguageText) {
  return { intent: null, params: {}, confidence: 0, note: "parser_not_implemented_v1" };
}

module.exports = { parseIntentPlaceholder };
