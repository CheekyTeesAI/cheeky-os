"use strict";

/**
 * AI Operator Layer — config surface (additive; no secrets).
 */
module.exports = {
  operatorLayerVersion: "3.0.0-phase1",
  /** When true, router uses operatorResponse envelopes only */
  standardizedEnvelope: true,
};
