refactor(service-layer): Phase 4+5 - pure extractions, zero new logic

Phase 4 (corrected surgical restart):
CHEEKY_advanceOrderSmart(id, routeFn) in orderService.js.
Removed out-of-scope CHEEKY_routeOrder. productionRoutingForOrder
stays in routes/orders.js and is passed as dependency to service fn.
Route handler is now a thin delegator (7 lines).

Phase 5 - HIGH VALUE route service extractions:
leadService.js: CHEEKY_triggerLeadFollowup - extracted from
  POST /api/leads/:id/followup (was direct prisma.lead.findUnique)
leadService.js: CHEEKY_convertLead - extracted from
  POST /api/leads/:id/convert (full lead-to-order conversion flow)
orderService.js: CHEEKY_listPayableOrders - extracted from
  GET /api/payments (square invoice order query + mapping)

All 5 route handlers now delegate to service layer.
Zero new business logic. Zero deletions. CHEEKY_ prefix on all new fns.
