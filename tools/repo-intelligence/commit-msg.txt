fix(schema+service): Phase 3 - orphan schema fix + service layer extraction

GAP 4 - Fix orphaned Prisma schema:
email-intake/src/db/schema.prisma had no datasource or generator block.
Added postgresql datasource + client generator so it passes prisma validate.
Annotated as legacy with reference to canonical schema.

GAP 1 (partial) - Service layer extraction for GET /api/orders:
Added CHEEKY_listOrders() to orderService.js. Routes GET /api/orders
now delegates to service layer instead of calling prisma.order.findMany()
directly in the handler. Prisma logic preserved inside service function.

Zero lines deleted. Zero schema model changes. CHEEKY_ prefix on new fn.
